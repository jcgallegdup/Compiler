void main() {
    boolean bool;
    int a;
    float b;

    // 3.1.1
    if (true) {
        print "true if statement";
    }

    // 3.1.2
    bool = true;
    while (true) {
        print "looping while once";
        bool = false;
    }

    // 3.1.3 & 3.1.4
    print 5;
    println "new line";

    // 3.1.6
    b = 0.0;
    return;
}
